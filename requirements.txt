# Шаг 0 (зеро-шот 107m R1) — зависимости эвал-харнесса бенчмарка.
# Прогон идёт через src.eval.run_dense из Kaz-RAG-search-benchmark,
# поэтому здесь минимальный набор для dense-инференса Granite R1.
torch>=2.1
transformers>=4.44
sentence-transformers>=3.0
numpy>=1.24

# Шаг д (train.py, Kaggle) — обучение через sentence-transformers Trainer:
datasets>=2.19
accelerate>=0.30
# Примечания:
#  • check_overlap.py (Шаг г) — на чистом stdlib, доп. зависимостей не требует.
#  • prepare_data.py (Шаг в) — тоже stdlib (gzip/json).
#  • Гибрид ⊕BM25 на Шаге е сделаем через eval-харнесс бенчмарка (rank-bm25 там уже есть).
