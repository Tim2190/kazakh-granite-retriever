# Granite-278m-kk — казахский retrieval на дообученном IBM Granite

Компактная (278M) embedding-модель для казахского поиска и RAG, дообученная из
`ibm-granite/granite-embedding-278m-multilingual` (R1). Казахского языка в
официальном списке Granite **нет** — модель показывает, что таргетированное
дообучение закрывает это на практике: она обходит специализированные казахские
модели и заметно улучшает базу, оставаясь лёгкой и воспроизводимой.

---

## Итог одним взглядом

- **Обходит специализированные казахские файнтюны** (kazakh-e5, kazembed-v5) на
  обоих проверочных доменах.
- **Гибрид с BM25 обходит прежний референс-топ** kazakh-e5 ⊕ BM25 (0.814 vs 0.808)
  и базовый multilingual-e5-base (0.785) на основном бенчмарке.
- **Улучшает базовый Granite-R1** значимо: Wiki 0.672 → 0.752 (dense) / 0.814 (hybrid).
- **Прирост обобщается** на независимый OOD-домен (официальные речи) — значимо по
  всем срезам.
- Компактная (278M), обучение на одном T4 (~1ч45м), всё воспроизводимо.

## Где модель в рейтинге (честно)

Основной бенчмарк [Kaz-RAG-search-benchmark](https://github.com/Tim2190/Kaz-RAG-search-benchmark)
(Википедия, 300 запросов, 8 370 пассажей), nDCG@10 (ALL):

| # | система | ALL |
|---|---|---|
| 1 | bge-m3 | 0.866 |
| 2 | jina-v3 | 0.821 |
| **3** | **Granite-278m-kk ⊕ BM25 (наш гибрид)** | **0.814** |
| 4 | kazakh-e5 ⊕ BM25 | 0.808 |
| 5 | cohere embed-v4 | 0.800 |
| 6 | multilingual-e5-base | 0.785 |
| 7 | BM25 + казахский стеммер | 0.754 |
| **8** | **Granite-278m-kk (наш, dense)** | **0.752** |
| 9 | kazakh-e5 (specialized) | 0.747 |
| 10 | Granite-R1 278m (база, zero-shot) | 0.672 |

Модель занимает верхний сегмент: dense-версия обходит специализированную казахскую
kazakh-e5, а гибрид с BM25 — её гибрид и базовый e5. Сильные общие мультиязычные
модели (bge-m3, jina-v3) впереди — это ориентир для дальнейшей работы.

## Как построена

Полный пайплайн — 8 скриптов в `scripts/`.

1. **Выбор базы (`zeroshot_107m.py`).** Zero-shot всех Granite. Для флагмана взят
   **278m R1** (сильнейший Granite zero-shot, 0.672).
2. **Данные (`prepare_data.py`).** KazQAD (казахская Википедия, CC BY-SA): 825K
   пассажей + размеченные тройки → 3 893 пары query→gold + hard-negatives (rel=0).
3. **Антилик (`check_overlap.py`).** Дедуп train против оценочного бенчмарка (оба из
   казвики) по заголовку + near-dup + article_id → **3 733 чистые пары**.
4. **Масштабирование (`generate_synthetic.py`).** Синтетическая генерация вопросов
   (Gemini) на пассажах казвики, сбалансированно по типам, со строгим антиликом.
   Итог: **40 084 пары** из 11 929 разных статей.
5. **Обучение (`train.py`).** sentence-transformers, CachedMNRL, 278m, 2 эпохи,
   lr 1e-5, max_seq_len 256, fp32, Kaggle T4. Негативы: in-batch (для синтетики) +
   размеченные KazQAD-негативы (`rel=0`) для gold-пар.
6. **Оценка (`eval.py`, `eval_ood.py`).** Тот же харнесс и метрики, что у бенчмарка,
   paired bootstrap (10k). Гибрид — RRF(dense, BM25 + казахский стеммер).

> **Что не вошло в финальную модель.** Казахский стеммер используется **только в
> BM25-канале гибрида** на этапе оценки, а не при обучении. Отдельно исследовался
> BM25-майнинг hard-negatives к обучающим парам (`mine_hard_negatives.py`, версия на
> identity-стеммере): на 14.7K он **не дал прироста** по ALL и добавил регрессию на
> inflected — поэтому в финальный рецепт не включён. Решающий рычаг — объём синтетики.

## Результаты — основной бенчмарк (Википедия)

**Zero-shot vs дообученная (nDCG@10):**

| срез | zero-shot | fine-tuned | Δ | p |
|---|---|---|---|---|
| **ALL** | 0.671 | **0.752** | +0.081 | **<0.001** |
| natural | 0.920 | 0.929 | +0.008 | 0.300 |
| inflected | 0.791 | 0.766 | −0.025 | 0.128 |
| vocabulary-gap | 0.301 | 0.562 | +0.260 | <0.001 |

Значимый прирост по ALL, natural сохранён (даже вырос), без значимых регрессий.

**Гибрид с BM25 (казахский стеммер):**

| срез | dense | BM25(kaz) | **гибрид** |
|---|---|---|---|
| **ALL** | 0.752 | 0.757 | **0.814** |
| natural | 0.929 | 0.772 | 0.884 |
| inflected | 0.766 | 0.736 | 0.805 |
| vocabulary-gap | 0.562 | 0.764 | 0.752 |

Гибрид 0.814 — выше kazakh-e5 ⊕ BM25 (0.808) и e5-base (0.785).

**Ключевое сравнение с казахскими файнтюнами:** kazakh-e5 (0.747) и kazembed-v5
(0.642) — специализированные казахские дообучения multilingual-e5 — оказываются
**ниже** нашей модели (0.752) и ниже своей же базы e5 (0.785). Наш файнтюн, наоборот,
**улучшил** базу. Вывод: правильно нацеленный файнтюн эффективнее «наивного» казахского.

## Результаты — независимая проверка (OOD: официальные речи, Akorda)

Второй бенчмарк [RAG-Two-Pass-Retrieval-QAZ](https://github.com/Tim2190/RAG-Two-Pass-Retrieval-QAZ):
471 пассаж речей akorda.kz / nazarbayev.kz — **другой домен**, модель на нём не
обучалась. Проверка обобщения.

**Zero-shot vs дообученная — все срезы значимо вверх:**

| срез | zero-shot | fine-tuned | Δ | p |
|---|---|---|---|---|
| **ALL** | 0.428 | 0.507 | +0.079 | <0.001 |
| factoid | 0.549 | 0.659 | +0.110 | <0.001 |
| paraphrase | 0.406 | 0.474 | +0.068 | 0.019 |
| low_overlap | 0.332 | 0.389 | +0.057 | 0.027 |

Прирост переносится на другой домен по всем типам запросов, включая честно-семантические
(paraphrase, low_overlap) — это реальный навык, а не подгонка под первый бенчмарк.
На Akorda наша модель (dense 0.507, гибрид 0.552) обходит базовый Granite-R1 (0.431)
и оба казахских файнтюна (shyngys/kazakh-e5 0.426, kazembed-v5 0.389).

## Использование

```python
from sentence_transformers import SentenceTransformer
m = SentenceTransformer("<HF-репозиторий модели>")   # ссылка будет добавлена
emb = m.encode(["Балқаш көлі қайда орналасқан?"])     # без спец-префиксов
```

Для сильнейшей связки — гибрид с BM25 (RRF), см. `eval.py --hybrid` и `scripts/`.

## Данные, лицензия, воспроизводимость

- **Обучающие данные:** `data/synthetic_pairs.jsonl` (40 084 пары) — синтетика на
  пассажах KazQAD (казахская Википедия, **CC BY-SA 4.0**, атрибуция KazQAD).
- **Скрипты:** `zeroshot_107m.py`, `prepare_data.py`, `check_overlap.py`,
  `generate_synthetic.py`, `mine_hard_negatives.py`, `train.py`, `eval.py`, `eval_ood.py`.
- **Отчёты по прогонам:** `results/*.md`.

## Методологические заметки

- Категория `vocabulary-gap` основного бенчмарка по валидации имеет высокое лексическое
  пересечение с ответом (описательные вопросы с редкими ключевыми словами), поэтому её
  корректнее читать как «различение близких сущностей», а не «понимание синонимов».
  Чистое семантическое подтверждение берётся из OOD-срезов paraphrase / low_overlap.
- Все сравнения — тем же харнессом и метриками, что и у бенчмарка; значимость — paired
  bootstrap (10 000 resamples).
