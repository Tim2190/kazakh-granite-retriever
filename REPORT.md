# Granite-278m-kk — казахский retrieval на дообученном IBM Granite

*English version — [REPORT_EN.md](REPORT_EN.md).*

Компактная (278M) embedding-модель для казахского поиска и RAG, дообученная из
`ibm-granite/granite-embedding-278m-multilingual` (R1). Казахского языка в
официальном списке Granite **нет** — модель показывает, что таргетированное
дообучение закрывает это на практике: значимо улучшает базу, обобщается на чужой
домен и идёт вровень со специализированными казахскими моделями.

Все числа — на **seq 512**, тем же харнессом и метриками, что и у бенчмарка,
значимость — paired bootstrap (10 000 resamples). Текущая модель — **v2**;
предыдущая доступна как `revision="v1"`.

---

## Итог одним взглядом (честно)

- **Значимо улучшает базовый Granite-R1** — Wiki 0.672 → **0.751** (dense) / **0.813**
  (hybrid), p<0.001; OOD (речи) 0.430 → **0.529**, все срезы p<0.05.
- **Вровень со специализированным kazakh-e5** — по ALL статистическая ничья (0.751 vs
  0.747, p=0.42). Обгона нет.
- **v2 значимо крепче v1 на морфологии** (inflected 0.752 → 0.792, p=0.002) — эффект
  hard-negatives со стеммером. Этим **закрыто единственное значимое преимущество
  kazakh-e5** (морфология: было p=0.001 в его пользу → стало p=0.06, ничья).
- **Прирост обобщается** на независимый OOD-домен, включая честно-семантические срезы.
- Компактная (278M), обучение на одном Kaggle T4, всё воспроизводимо.

## Где модель в рейтинге (честно)

Основной бенчмарк [Kaz-RAG-search-benchmark](https://github.com/Tim2190/Kaz-RAG-search-benchmark)
(Википедия, 300 запросов, 8 370 пассажей), nDCG@10 (ALL), @512. Парного теста между
чужими системами у нас нет, поэтому их числа — ориентир, а не строгий рейтинг:

| система | ALL |
|---|---|
| bge-m3 | ~0.866 |
| jina-v3 | ~0.821 |
| **Granite-278m-kk ⊕ BM25 (наш гибрид)** | **0.813** |
| kazakh-e5 ⊕ BM25 | ~0.808 |
| multilingual-e5-base | ~0.785 |
| BM25 + казахский стеммер | 0.757 |
| **Granite-278m-kk (наш, dense)** | **0.751** |
| kazakh-e5 (specialized) | 0.747 |
| Granite-R1 278m (база, zero-shot) | 0.672 |

Модель в верхнем сегменте: гибрид в одном ряду с kazakh-e5 ⊕ BM25, dense — на уровне
kazakh-e5 (парная ничья). Сильные общие модели (bge-m3, jina-v3) впереди — ориентир
для дальнейшей работы. **Мы НЕ утверждаем, что обходим kazakh-e5** — по ALL это ничья.

## Бенчмарки и источники

- **[Kaz-RAG-search-benchmark](https://github.com/Tim2190/Kaz-RAG-search-benchmark)** —
  основной бенчмарк (казахская Википедия). Источник сравнительного рейтинга и
  eval-харнесса: `src/retrieval` (DenseIndex, BM25 + казахский стеммер), `src/eval`
  (метрики, paired bootstrap). Наши `eval.py` / `eval_ood.py` импортируют его напрямую.
- **[RAG-Two-Pass-Retrieval-QAZ](https://github.com/Tim2190/RAG-Two-Pass-Retrieval-QAZ)** —
  независимый OOD-бенчмарк (официальные речи akorda.kz / nazarbayev.kz).
- **[KazQAD](https://github.com/IS2AI/KazQAD)** — обучающие пассажи (казахская Википедия,
  CC BY-SA 4.0).

## Как построена

1. **Выбор базы (`zeroshot_107m.py`).** Zero-shot всех Granite. Флагман — **278m R1**
   (сильнейший Granite zero-shot, 0.672).
2. **Данные (`prepare_data.py`).** KazQAD: 825K пассажей + размеченные тройки → 3 893
   пары query→gold + hard-negatives (rel=0).
3. **Антилик (`check_overlap.py`).** Дедуп train против бенчмарка по заголовку +
   near-dup + article_id.
4. **Синтетика (`generate_synthetic.py`).** Генерация вопросов (Gemini) на пассажах
   казвики, сбалансированно по типам, со строгим антиликом → **57 369 пар** (после
   дедупа) из ~19K статей.
5. **Hard-negatives (`mine_hard_negatives.py`).** Для каждого запроса — топ BM25 с
   **казахским стеммером** минус gold → морфологические «обманки» (по 1 на пару).
   Именно этот шаг отличает v2 от v1.
6. **Сборка train-файла (`build_train_v2.py`).** id → тексты из корпуса + KazQAD gold
   = 61 102 обучающих примера.
7. **Обучение (`train.py`).** sentence-transformers, CachedMNRL, 278m, 2 эпохи,
   lr 1e-5, **max_seq_len 512**, fp16-сейв, Kaggle T4 (~7.5 ч на 2×T4).
8. **Оценка (`eval.py`, `eval_ood.py`).** Тот же харнесс и метрики, paired bootstrap
   (10k), строго на seq 512. Гибрид — RRF(dense, BM25 + казахский стеммер).

## Результаты — основной бенчмарк (Википедия), @512

**Zero-shot vs v2 (dense, nDCG@10):**

| срез | zero-shot | v2 | Δ | p |
|---|---|---|---|---|
| **ALL** | 0.672 | **0.751** | +0.079 | **<0.001** |
| natural | 0.923 | 0.928 | +0.004 | 0.384 |
| inflected | 0.791 | 0.792 | +0.002 | 0.475 |
| vocabulary-gap | 0.303 | 0.534 | +0.231 | **<0.001** |

**Гибрид с BM25 (казахский стеммер):**

| срез | dense | BM25(kaz) | **гибрид** |
|---|---|---|---|
| **ALL** | 0.751 | 0.757 | **0.813** |
| natural | 0.928 | 0.772 | 0.888 |
| inflected | 0.792 | 0.736 | 0.822 |
| vocabulary-gap | 0.534 | 0.764 | 0.728 |

**v2 vs kazakh-e5 (paired bootstrap):**

| срез | v2 | kazakh-e5 | Δ | p |
|---|---|---|---|---|
| **ALL** | 0.751 | 0.747 | +0.004 | 0.419 (ничья) |
| inflected | 0.792 | 0.836 | −0.044 | 0.063 (ничья) |
| natural | 0.928 | 0.909 | +0.019 | 0.213 |
| vocabulary-gap | 0.534 | 0.497 | +0.037 | 0.168 |

По ALL — ничья. Важно: у v1 kazakh-e5 бил нас на inflected **значимо** (p=0.001); у v2
этот разрыв **перестал быть значимым** (p=0.063) — то есть морфологическое преимущество
специализированной модели закрыто.

**v2 vs v1 (что дал раунд со стеммер-негативами):**

| срез | v2 | v1 | Δ | p |
|---|---|---|---|---|
| ALL | 0.751 | 0.742 | +0.009 | 0.084 |
| **inflected** | **0.792** | **0.752** | **+0.040** | **0.002** |
| natural | 0.928 | 0.921 | +0.007 | 0.106 |
| vocabulary-gap | 0.534 | 0.553 | −0.019 | 0.055 |

**Значимо** улучшилась только морфология (inflected, p=0.002) — ровно то, куда целили
стеммер-негативы. По ALL прирост номинальный, но незначимый (p=0.084). vocab-gap чуть
просел (p=0.055, на грани) — честный trade-off: усилив морфологию, слегка отъели на
различении близких сущностей.

## Результаты — независимая проверка (OOD: речи, Akorda), @512

Второй бенчмарк [RAG-Two-Pass-Retrieval-QAZ](https://github.com/Tim2190/RAG-Two-Pass-Retrieval-QAZ):
471 пассаж речей akorda.kz / nazarbayev.kz — **другой домен**, модель на нём не
обучалась.

| срез | zero-shot | v2 | Δ | p |
|---|---|---|---|---|
| **ALL** | 0.430 | 0.529 | +0.099 | <0.001 |
| factoid | 0.548 | 0.680 | +0.132 | <0.001 |
| paraphrase | 0.406 | 0.503 | +0.097 | 0.004 |
| low_overlap | 0.339 | 0.405 | +0.066 | 0.009 |

Гибрид на Акорде — 0.554. Прирост держится **на чужом домене** и на честно-семантических
срезах (paraphrase, low_overlap) — это реальный навык, а не подгонка под бенчмарк №1.
Против v1 (dense 0.507 / hybrid 0.552) v2 номинально чуть выше (0.529 / 0.554); парного
теста на OOD нет, поэтому — «не хуже», без сильных заявлений.

## Перспективы

Эксперимент удался: дообучение превратило официально «неподдерживаемый» Granite в
конкурентный казахский ретривер, идущий вровень со специализированными моделями и
значимо улучшающий базу на двух доменах. Рычаги дальше:

1. **Больше hard-negatives на пару** (v2 — по 1; 2–4 могут дожать морфологию/ALL).
2. **Больше синтетики** (57K → 100K+; корпус KazQAD 825K пассажей позволяет).
3. **Вернуть vocab-gap** — сбалансировать негативы, чтобы не терять на различении
   близких сущностей.
4. **Гибрид как единый компонент** — обернуть Granite-ft + BM25(стеммер) + RRF в один
   `.search()`.
5. **Разбор отрыва bge-m3 / jina-v3** — что даёт сильным общим моделям преимущество.

## Использование

```python
from sentence_transformers import SentenceTransformer
m = SentenceTransformer("Tim2190/granite-278m-kk")   # 🤗 fp16, ~556 МБ (v2)
emb = m.encode(["Балқаш көлі қайда орналасқан?"])     # без спец-префиксов
# предыдущая версия: SentenceTransformer("Tim2190/granite-278m-kk", revision="v1")
```

Для сильнейшей связки — гибрид с BM25 (RRF), см. `eval.py --hybrid` и `scripts/`.

## Данные, лицензия, воспроизводимость

- **Обучающие данные:** `data/synthetic_pairs.jsonl` (57 369 пар) + hard-negatives
  (`data/synthetic_pairs.hn.ids.jsonl`) + KazQAD gold (`data/kazqad_pairs.dedup.jsonl`) —
  синтетика на пассажах KazQAD (казахская Википедия, **CC BY-SA 4.0**, атрибуция KazQAD).
- **Скрипты:** `zeroshot_107m.py`, `prepare_data.py`, `check_overlap.py`,
  `generate_synthetic.py`, `mine_hard_negatives.py`, `build_train_v2.py`, `train.py`,
  `eval.py`, `eval_ood.py`. Ноутбуки прогонов — `notebooks/`.
- **Модель:** [`Tim2190/granite-278m-kk`](https://huggingface.co/Tim2190/granite-278m-kk)
  (fp16, ~556 МБ; v2 в `main`, v1 под тегом `v1`).

## Методологические заметки

- Категория `vocabulary-gap` основного бенчмарка по валидации имеет высокое лексическое
  пересечение с ответом, поэтому её корректнее читать как «различение близких сущностей»,
  а не «понимание синонимов». Чистое семантическое подтверждение — из OOD-срезов
  paraphrase / low_overlap.
- Все сравнения — тем же харнессом и метриками, что и у бенчмарка, на seq 512; значимость
  — paired bootstrap (10 000 resamples).
